<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.tianqi.plankton.system.mapper.UserDao">
    <select id="findUser" resultType="top.tianqi.plankton.system.entity.User">
        select u.* from user u
        <where>
            <if test="imel != null">
                u.imel = #{imel}
            </if>
        </where>

    </select>
    <select id="findPage" resultType="top.tianqi.plankton.system.entity.User">
        select * from user u where 1=1
        <if test="ieml != null">
           and u.ieml like '%#{ieml}%'
        </if>
        <if test="username != null ">
           and u.username like '%#{username}%'
        </if>
    </select>
</mapper>